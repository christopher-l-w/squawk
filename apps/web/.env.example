# Vite reads env files from apps/web/. Copy to `.env` for local dev.

# Squawk API base URL (no trailing slash); include the /v1 API prefix.
VITE_API_URL=http://localhost:3001/v1

# Production deploy: copy `.env.production.example` to `.env.production` and run `npm run build:web` from the repo root.
