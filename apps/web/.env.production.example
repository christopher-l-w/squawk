# Copy to `.env.production` in this directory (gitignored).
# Used automatically by `vite build` (production mode).

# Public Squawk API base, including /v1 (no trailing slash after v1).
VITE_API_URL=https://api.christopherw.ca/v1
