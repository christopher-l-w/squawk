/** httpOnly cookie carrying the raw opaque session token */
export const SESSION_COOKIE_NAME = 'squawk_session'

/** Session lifetime */
export const SESSION_MAX_AGE_SEC = 60 * 60 * 24 * 14

export const SESSION_MAX_MS = SESSION_MAX_AGE_SEC * 1000
